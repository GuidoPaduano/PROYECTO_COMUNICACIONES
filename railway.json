{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd boletin && pip install -r requirements.txt"
  },
  "start": "cd boletin && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn boletin.wsgi"
}


