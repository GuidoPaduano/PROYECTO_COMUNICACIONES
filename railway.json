{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "start": "python manage.py migrate && daphne -b 0.0.0.0 -p ${PORT:-8000} boletin.asgi:application"
}
